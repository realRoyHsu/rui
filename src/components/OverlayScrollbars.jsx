import React, { Component } from 'react';
import PropTypes from 'prop-types';
import OverlayScrollbars from 'overlayscrollbars';

export default class OverlayScrollbarsImpl extends Component {
  static propTypes = {
      prop: PropTypes,
  }

  render() {
      return (
          <div>

          </div>
      );
  }
};
